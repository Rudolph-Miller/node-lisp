{
	"author": "Rudolph-Miller",
		"name": "node-lisp",
		"version": "1.0.5",
		"keywords": ["common lisp", "lisp", "coffeescript"],
		"description": "connection of lisp. before use you have to install sbcl",
		"repository": {
			"type": "git",
			"url": "git://github.com/Rudolph-Miller/node-lisp"
		},
		"scripts": {
		  "prepublish": "coffee -o lib/ -c src/*.coffee",
		  "build": "coffee -o lib/ -c src/*.coffee"
		},
		"main": "lib/node-lisp.js",
		"engines": {
			"node":"*"
		},
		"dependencies": {
		},
		"devDependencies": {
			"coffee-script":"*"
		}
}
